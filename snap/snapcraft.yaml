name: aero
base: core18
version: '0.0.1'
summary: A task runner for Python
description: Aero is a task runner made with the Python scripting language.

grade: stable
confinement: strict

parts:
  binary:
    # See 'snapcraft plugins'
    plugin: dump
    source: https://github.com/X-Store-App/aero/releases/download/v0.0.1/aero
